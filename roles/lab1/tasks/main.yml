---

  - name: SELETOR DE LINK
    include_tasks: "{{ link }}.yml"
    # when: bgp_shut is not defined
    file: install.yml
    apply:
      tags:
        - atributos
    tags: allways

  - name: EXECUTANDO TAREFA SHUTDOWN
    include_tasks: "BGP_shutdown.yml"
    # when: bgp_shut is defined 
    tags: shut 